eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
#  endpoint:
#    health:
#      show-details: always
info:
  author: Israel Almeida
  mail: israelalmeida31@gmail.com
  cel: (99) 99999-9999
server:
  port: ${PORT:9999}
message: Mensagem generica
